# 如果模型下载一直失败，使用这个配置
# 模拟 Embedding 模式（系统可用，但匹配质量会降低）

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-key
SECRET_KEY=your-secret

# AI 模型（必须配置）
AI_PROVIDER=longcat
LONGCAT_API_KEY=your-key
LONGCAT_BASE_URL=https://api.ai.meituan.com/v1

# Embedding - 模拟模式（无需下载模型）
EMBEDDING_MODE=local
EMBEDDING_MODEL_NAME=mocker
EMBEDDING_DIMENSION=512

# 注意：模拟模式下，所有 Embedding 都是随机生成的
# 匹配功能可用，但准确性较低
# 建议后续手动下载模型后切换回正常配置
